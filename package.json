{
  "name": "webhawk",
  "version": "0.1.0",
  "description": "Real-time webhook security auditor proxy — HMAC verification, replay protection, rate limiting on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20240821.1",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.0",
    "wrangler": "^3.75.0"
  },
  "dependencies": {
    "hono": "^4.5.11",
    "ipaddr.js": "^2.5.0"
  }
}
